# This is only read by non-bash shells or if .bash_profile is not available.
# I only expect that to happen for pre-packaged scripts without environmental requirements.
# Because of that I keep a blank profile.
. "$HOME/.cargo/env"
